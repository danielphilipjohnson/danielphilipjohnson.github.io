---
import StackBadge from "./StackBadge.astro";
const { title, items = [] } = Astro.props;
---

<section class="grid gap-2">
  <h3 class="text-[length:var(--fs-0)] font-semibold">{title}</h3>
  <div class="flex flex-wrap gap-2">
    {items.map((i) => <StackBadge {...i} />)}
  </div>
</section>
